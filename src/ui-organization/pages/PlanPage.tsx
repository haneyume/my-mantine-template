import { Stack, Card, Title } from '@mantine/core';

export const PlanPage = () => {
  return (
    <Stack>
      <Card withBorder>
        <Stack>
          <Title order={3}>Plan</Title>
        </Stack>
      </Card>
    </Stack>
  );
};
